[tool.poetry]
name = "routrs-py"
version = "0.1.0"
description = "Geograph-based shortest distance calculation"
authors = ["Carlo Casorzo <carlo@deepblu.dev>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[tool.poetry.group.dev.dependencies]
ruff = "^0.3.7"
maturin = "^1.5.1"
pytest = "^8.1.1"
mypy = "^1.9.0"

[build-system]
requires = ["maturin>=0.12,<0.13"]
build-backend = "maturin"
